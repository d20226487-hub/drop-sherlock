import Link from "next/link";

export const metadata = { title: "Задачи — Drop Sherlock" };

export default function JobsDoc() {
  return (
    <div className="docs-content">
      <h1>Задачи</h1>
      <p>
        <strong>Задача</strong> (Job) — это контейнер для одного или
        нескольких запусков (Runs). Один сабмит из формы Анализа создаёт
        Job + первый Run. Дальше внутри Job можно делать reruns с другими
        настройками и сравнивать их между собой.
      </p>

      <h2>Список задач</h2>
      <p>
        Страница <code>/jobs</code> — список всех Job. Каждый ряд: имя,
        дата, число запусков, статусные плашки. Внизу — пагинация.
      </p>
      <p>
        Архивированные задачи скрыты по умолчанию. Переключатель{" "}
        <strong>«Archived»</strong> сверху показывает только архив или
        всё подряд. Архив — это «убрать с глаз», без удаления. Восстановить
        можно с самой страницы задачи.
      </p>

      <h2>Страница одной задачи</h2>
      <p>В шапке:</p>
      <ul>
        <li>Имя задачи (редактируется кнопкой Rename).</li>
        <li>
          <strong>Плашки сводки</strong>: сколько доменов попали в каждый
          бакет (good / mixed / low quality), плюс partial и no_verdict.
          Источник сводки — закреплённый Run, иначе последний (см. ниже).
        </li>
        <li>Заметки задачи (свободный текст).</li>
        <li>
          Кнопки <strong>Rerun</strong>, <strong>Rename</strong>,{" "}
          <strong>Archive</strong>, <strong>Delete</strong>. Compare runs —
          появляется при двух и более запусках.
        </li>
      </ul>

      <h3>Закрепление запуска</h3>
      <p>
        У каждого запуска есть кнопка <strong>«Pin»</strong> (для
        завершённых, <code>done</code>). Один пин на задачу: повторное
        нажатие на другую строку перекидывает пин туда. Закрепление
        переключает источник сводки на этот запуск — удобно, когда:
      </p>
      <ul>
        <li>
          Последний запуск был частичным (только wayback_classify, или
          только 10 доменов из 200).
        </li>
        <li>
          У вас есть «эталонный» прогон, и rerun-ы — просто экспериментальные.
        </li>
      </ul>
      <p>
        В шапке появляется чип <code>📌 Pinned: Run #N</code>. Подробности
        в статье <Link href="/docs/pinning">«Закрепление»</Link>.
      </p>

      <h3>Per-criterion pins (read-only виджет)</h3>
      <p>
        Под заметками и над списком запусков — свёрнутая по умолчанию
        панель с шестью карточками{" "}
        (<code>B&nbsp;D&nbsp;A&nbsp;K&nbsp;W&nbsp;C</code>). Каждая
        карточка показывает, какой Run внутри этой Задачи закреплён
        как источник для соответствующего критерия в Базе. Зелёная
        рамка + ссылка «Run #N» — критерий закреплён; серая «not
        pinned» — Drop Sherlock возьмёт самый свежий Run, у которого
        есть данные по этому критерию.
      </p>
      <p>
        Виджет — только для чтения. Изменение пинов идёт с{" "}
        <Link href="/docs/run">страницы Run</Link>, секция Per-criterion
        pins. Здесь — кросс-обзор: «B ← Run #77, W ← Run #61, …», чтобы
        не ходить по каждому Run по очереди, когда хочется понять, из
        чего собрана строка Базы для домена этой Задачи.
      </p>
      <p>
        На бейдже в шапке панели видно «N/6 закреплено» — поэтому даже
        свёрнутая панель сразу показывает, насколько Задача укомплектована.
      </p>

      <h3>Сравнение запусков</h3>
      <p>
        Кнопка <strong>«Compare runs»</strong> доступна, когда в задаче
        больше одного запуска. Открывается матрица «домен × запуск →
        bucket / score / confidence». Каждая колонка-запуск имеет
        собственный фильтр; помогает увидеть:
      </p>
      <ul>
        <li>
          Какие домены поменяли бакет между запусками. Если поменялись
          сильно — повод посмотреть, что именно изменилось в промптах.
        </li>
        <li>
          Был ли A/B-тест информативным или ИИ выдал то же самое.
        </li>
        <li>
          Локальные регрессии: домен был good, стал low_quality — почему?
        </li>
      </ul>
      <p>
        В колонках видно, какой провайдер/модель использовался в каждом
        запуске — удобно для атрибуции «эта модель оценила злее».
      </p>

      <h2>Управление запусками</h2>
      <p>В строке каждого Run:</p>
      <ul>
        <li>
          <strong>Pause / Resume</strong> — корректно ставит/снимает паузу.
          Уже скачанные данные сохраняются.
        </li>
        <li>
          <strong>Cancel</strong> — отменяет ещё не обработанные домены.
        </li>
        <li>
          <strong>Rename</strong> — присваивает запуску читаемое имя
          («with gpt-4o», «after prompt v3»). Помогает в Compare runs не
          угадывать «какой run был с чем».
        </li>
        <li>
          <strong>Delete</strong> — навсегда. Доступно только для
          завершённых.
        </li>
        <li>
          <strong>Pin</strong> — см. выше.
        </li>
      </ul>

      <h2>Что считается «завершённым» запуском</h2>
      <p>
        Запуск имеет статус <code>done</code>, когда все его домены
        прошли через сбор данных и ИИ-судей (либо вернули вердикт, либо
        упали с ошибкой — без зависших). Только <code>done</code>-запуски
        можно закреплять для сводки.
      </p>
      <p>
        Если запуск упал с ошибкой Ahrefs или ИИ-провайдер вернул HTTP
        ошибку, домен попадёт в{" "}
        <Link href="/docs/errors">«Ошибки»</Link>. Обычно достаточно
        дождаться лимита API и нажать <strong>«Retry failed»</strong> на
        странице запуска — Drop Sherlock попытается доделать только
        упавшие критерии, не перепроверяя удачные.
      </p>
    </div>
  );
}
