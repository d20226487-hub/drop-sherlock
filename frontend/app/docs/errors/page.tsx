import Link from "next/link";

export const metadata = { title: "Ошибки — Drop Sherlock" };

export default function ErrorsDoc() {
  return (
    <div className="docs-content">
      <h1>Ошибки</h1>
      <p>
        Сюда попадают все сбои, которые Drop Sherlock зафиксировал во
        время сбора данных или вызовов ИИ. Страница помогает понять,
        почему конкретный домен или критерий не дал результата, и
        исправить причину перед retry.
      </p>

      <h2>Что лежит на странице</h2>
      <p>
        Журнал ошибок: время, тип, контекст (домен / критерий / Job /
        Run), краткое сообщение, raw-ответ провайдера (по клику
        раскрывается). Можно фильтровать по типу и периоду.
      </p>

      <h2>Откуда берутся записи</h2>
      <ul>
        <li>
          <strong>Ahrefs</strong>: HTTP-ошибки (401 — невалидный токен,
          429 — rate limit, 5xx — таймауты). Логируется URL запроса.
        </li>
        <li>
          <strong>ИИ-провайдер</strong>: ошибки авторизации, лимиты,
          модельные сбои (invalid JSON в ответе, отсутствие обязательных
          полей). Logged в raw_body.
        </li>
        <li>
          <strong>Парсинг</strong>: ИИ ответил, но в формате, который мы
          не смогли разобрать (например, обернул JSON в markdown без
          обрамляющего токена).
        </li>
        <li>
          <strong>Wayback</strong>: ошибки CDX или V2-сэмплинга
          (таймауты, 5xx).
        </li>
      </ul>

      <h2>Что делать после ошибки</h2>
      <ol>
        <li>
          Определите тип. Если 429/таймаут — обычно достаточно подождать
          и нажать <strong>«Retry failed»</strong> на странице{" "}
          <Link href="/docs/run">запуска</Link>.
        </li>
        <li>
          Если 401 / invalid key — проверьте ключи в{" "}
          <Link href="/docs/settings">Настройках</Link> → «API».
        </li>
        <li>
          Если ошибка парсинга и часто повторяется на одной модели —
          возможно, модель плохо держит JSON-формат. Попробуйте другую
          модель или подправьте промпт в{" "}
          <Link href="/docs/brain">Brain</Link> (усильте «No prose around
          the JSON»).
        </li>
        <li>
          Если ошибка Wayback persistent на одном домене — этот домен,
          скорее всего, не индексирован архивом. Запустите анализ без
          wayback-критериев.
        </li>
      </ol>

      <h2>Retention (хранение)</h2>
      <p>
        Журнал ошибок периодически чистится — старые записи удаляются
        автоматически. Период хранения настраивается в{" "}
        <Link href="/docs/settings">Настройках</Link> → «Other» →
        «Retention». По умолчанию задан разумный лимит, чтобы база не
        росла бесконечно.
      </p>

      <h2>Безопасность</h2>
      <p>
        В raw-ответах провайдеров иногда может оказаться часть API-ключа
        или пользовательский промпт. Журнал ошибок виден только в
        локальном LAN-деплое за basicauth — никаких внешних утечек, но
        стоит помнить, если будете кому-то делиться скриншотами.
      </p>
    </div>
  );
}
