import Link from "next/link";

export const metadata = { title: "ИИ — Drop Sherlock" };

export default function AiDoc() {
  return (
    <div className="docs-content">
      <h1>ИИ: провайдеры, модели, стоимость</h1>
      <p>
        Drop Sherlock работает с несколькими ИИ-провайдерами. Эта статья
        объясняет, как их добавить, как выбрать модель и как считается
        стоимость.
      </p>

      <h2>Провайдеры</h2>
      <p>
        Поддерживаемые на момент написания:
      </p>
      <ul>
        <li>
          <strong>OpenAI</strong> — gpt-4o, gpt-4o-mini и др. Стабильный
          формат JSON, хорошо держит инструкции.
        </li>
        <li>
          <strong>Anthropic</strong> — Claude 3.5 Sonnet / Haiku.
          Сильны на длинных контекстах и нюансах.
        </li>
        <li>
          <strong>Google Gemini</strong> — gemini-2.5-pro / flash. Самые
          дешёвые в пересчёте, но иногда хуже держат trailing-директивы (это
          один из поводов поддерживать{" "}
          <Link href="/docs/brain">localize_ru</Link> отдельно
          редактируемой).
        </li>
        <li>
          <strong>OpenRouter</strong> — прокси к десяткам моделей одной
          ключом. Удобно для экспериментов.
        </li>
        <li>
          <strong>DeepSeek</strong> — дешёвый китайский провайдер с
          неплохим качеством на технических задачах.
        </li>
        <li>
          <strong>Vertex AI</strong> — зарегистрирован, но реализация
          стоит в ожидании реального запроса (нужен GCP-сервис-аккаунт).
        </li>
      </ul>
      <p>
        Ключи задаются в <Link href="/docs/settings">Настройках</Link> →
        «API» → Providers. Drop Sherlock хранит их зашифрованно (Fernet)
        в SQLite.
      </p>

      <h2>Выбор модели</h2>
      <p>Модель выбирается в двух местах:</p>
      <ul>
        <li>
          В <Link href="/docs/analyze">форме Анализа</Link> — для нового
          запуска.
        </li>
        <li>
          В кнопках Reanalyze — для разовых переоценок (всего домена или
          отдельного критерия).
        </li>
      </ul>
      <p>
        Если в форме оставить «default», Drop Sherlock возьмёт первую
        модель из списка для выбранного провайдера. Это удобно, чтобы не
        думать про точное имя модели каждый раз.
      </p>

      <h3>Когда какую модель брать</h3>
      <ul>
        <li>
          <strong>Дешёвая (Haiku, Flash, gpt-4o-mini)</strong> — для
          больших триажей по 500+ доменам. Дисперсия выше, но цена в
          разы ниже. Подходит, когда вы уже определили хороший промпт.
        </li>
        <li>
          <strong>Средняя (Sonnet, gpt-4o)</strong> — для прицельных
          партий 50–100 доменов. Стабильность вердиктов лучше, разумная
          стоимость.
        </li>
        <li>
          <strong>Большая (Opus, gpt-4.1)</strong> — для финальной
          переоценки топ-10 кандидатов или для A/B-теста промптов на
          маленькой выборке. Дорого, но качественно.
        </li>
      </ul>

      <h2>Стоимость</h2>
      <p>
        Drop Sherlock считает стоимость на лету:
      </p>
      <pre><code>{`cost = (input_tokens / 1_000_000) * input_price
     + (output_tokens / 1_000_000) * output_price`}</code></pre>
      <p>
        Цены хранятся в таблице Pricing (Settings → API → Pricing). Каждая
        запись — «провайдер × модель → input_price, output_price». При
        каждом успешном вызове токены и долларовая стоимость записываются
        в строку CriterionResult (или RunDomain для final synth) — они
        фиксируются на момент вызова и <em>не пересчитываются</em>, если
        вы потом отредактируете цену в Pricing.
      </p>
      <p>Видимость стоимости:</p>
      <ul>
        <li>
          <strong>В строке запуска</strong> — суммарный $ + юниты Ahrefs.
        </li>
        <li>
          <strong>В странице запуска</strong> — детализированный список:
          сколько свежих ИИ-вызовов, сколько cache-hits, какие модели
          участвовали.
        </li>
        <li>
          <strong>В странице домена</strong> — стоимость на этот домен.
        </li>
      </ul>

      <h3>Пропуски цен</h3>
      <p>
        Если в таблице Pricing нет записи для какой-то модели, токены
        считаются, а стоимость — null. В UI появится пометка «Missing
        pricing for &lt;model&gt; — total is incomplete». Это сигнал
        добавить строку в Pricing, чтобы сводки были точными.
      </p>

      <h2>Кэш и стоимость</h2>
      <p>
        Если вердикт прилетел из кэша (см.{" "}
        <Link href="/docs/cache">«Кэш»</Link>), его стоимость в новом
        запуске считается как $0 — вы за этот вердикт уже заплатили в
        исходном запуске. Токены при этом видны (для прозрачности), просто
        $-стоимость = 0.
      </p>

      <h2>Лимиты и rate-limiting</h2>
      <p>
        Drop Sherlock держит per-provider concurrency и RPM-лимиты,
        чтобы не упереться в провайдеровские квоты на одном тяжёлом
        прогоне. Лимиты задаются на стороне бэкенда (в коде); если
        упираетесь в реальный rate limit от провайдера, провайдер
        ответит 429, домен с этим критерием попадёт в{" "}
        <Link href="/docs/errors">Ошибки</Link>, и вы сможете нажать{" "}
        <strong>«Retry failed»</strong> на странице запуска.
      </p>
    </div>
  );
}
