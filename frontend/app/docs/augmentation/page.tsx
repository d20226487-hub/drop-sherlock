import Link from "next/link";

export const metadata = { title: "Augmentation chain — Drop Sherlock" };

export default function AugmentationDoc() {
  return (
    <div className="docs-content">
      <h1>Augmentation chain</h1>
      <p>
        Augmentation — это механизм, который позволяет запускать{" "}
        <em>частичный</em> анализ (только пара критериев) поверх уже
        собранного полного, не теряя контекста. Например, у вас есть
        старый полный анализ домена со всеми Ahrefs-критериями, и вы
        хотите только перепроверить Wayback Classify после правки
        промпта. Без augmentation страница домена выглядела бы
        «обрезанной»; с augmentation Drop Sherlock склеивает картину из
        двух RunDomain.
      </p>

      <h2>Когда срабатывает</h2>
      <p>
        Augmentation активируется автоматически, когда вы запускаете
        задачу с <em>строгим подмножеством</em> критериев от ранее
        существовавшего полного RunDomain того же домена. «Строгое
        подмножество» означает: все критерии нового запуска уже были у
        родительского RunDomain, плюс родитель имел минимум один
        критерий, которого нет в новом.
      </p>
      <p>
        Drop Sherlock проставляет на новой RunDomain поле{" "}
        <code>augments_rd_id</code>, ссылающееся на родителя. На странице
        домена видна плашка <em>«Augments Run #N»</em> со ссылкой.
      </p>

      <h2>Что подмешивается</h2>
      <p>
        На странице домена для критериев, которые есть{" "}
        <em>только у родителя</em>, отображаются данные родителя — с
        чипом <em>«from Run #N»</em>, чтобы визуально было понятно «эти
        строки пришли не из текущего run, а из augmented».
      </p>
      <p>
        Если у нового RunDomain нет final assessment (например, частичный
        запуск ещё не получил wayback_classify), а родительский RunDomain
        имеет полный final — баннер на странице домена подтянет финал из
        родителя. С пометкой «from Run #N — this run was partial.»
      </p>

      <h2>Чем отличается от кэша</h2>
      <table>
        <thead>
          <tr>
            <th></th>
            <th>Augmentation</th>
            <th><Link href="/docs/cache">Cache</Link></th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Связь между RunDomain'ами</td>
            <td>
              Явная — <code>augments_rd_id</code> указывает на родителя
            </td>
            <td>Нет — строка копируется и стоит автономно</td>
          </tr>
          <tr>
            <td>Когда срабатывает</td>
            <td>Спец автоматика при «строгом подмножестве» критериев</td>
            <td>
              На любом совпадении params_hash + prompt_hash в кэше
            </td>
          </tr>
          <tr>
            <td>Где это видно</td>
            <td>Чип «Augments Run #N» в шапке домена</td>
            <td>Фиолетовая пилюля «Reused from Run #N» в таблицах</td>
          </tr>
          <tr>
            <td>Зачем</td>
            <td>
              Сохранить целостность картины при частичных reruns без
              перекачивания старых данных
            </td>
            <td>
              Экономия юнитов/токенов при одинаковых параметрах
            </td>
          </tr>
        </tbody>
      </table>

      <h2>Когда вам это пригодится</h2>
      <ul>
        <li>
          <strong>Доуточнение классификации.</strong> У вас 200 доменов с
          полным анализом, но Wayback Classify ещё нужен с правкой
          промпта. Делаете rerun с включённым только{" "}
          <code>wayback_classify</code>. Новый RunDomain каждого домена
          augment'ит старый. В Базе домен по-прежнему доступен с
          полными данными.
        </li>
        <li>
          <strong>Эксперимент с одной моделью.</strong> Хотите
          пересчитать только <code>anchors</code> на новой модели. Rerun с
          выключенными прочими критериями. Старые вердикты по другим
          критериям остаются доступны через augmentation.
        </li>
      </ul>

      <h2>Ограничения</h2>
      <ul>
        <li>
          Augmentation идёт ровно <em>на один хоп</em>. Если у вас цепочка
          rd → rd' (augmented) → rd'' (augmented от rd'), фактически
          доступны данные только rd → rd''. Drop Sherlock не делает
          транзитивной склейки, чтобы не создавать двусмысленностей.
        </li>
        <li>
          Augmentation работает только при <em>строгом</em> подмножестве.
          Если критерии частично пересекаются (например, новый запуск имеет
          <code>anchors</code>, которого не было у родителя), считается
          новым полным запуском — augmentation не активируется.
        </li>
        <li>
          Closing the loop: если вы потом сделаете полный rerun (со всеми
          критериями), augmentation chain прервётся — новый RunDomain
          автономен.
        </li>
      </ul>

      <div className="callout callout-info docs-content">
        <p>
          <strong>Подсказка:</strong> большинству пользователей знание об
          augmentation не нужно — оно работает прозрачно. Эту статью имеет
          смысл прочитать, если на странице домена вы видите чип «Augments
          Run #N» и хотите понять, что он означает.
        </p>
      </div>
    </div>
  );
}
