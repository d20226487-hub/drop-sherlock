import Link from "next/link";

export const metadata = { title: "Wayback History — Drop Sherlock" };

export default function WaybackDoc() {
  return (
    <div className="docs-content">
      <h1>Wayback History</h1>
      <p>
        Wayback History — критерий, который тянет историю краулов домена из
        Internet Archive (CDX) и, опционально, скачивает несколько архивных
        страниц целиком, чтобы ИИ-судья мог сравнить заголовки и контент
        год-к-году. Закрывает три слепых зоны Ahrefs:
      </p>
      <ul>
        <li>
          <strong>Хвост из 301/302 в недавних снимках</strong> — оригинальный
          сайт уехал на другой ресурс. Перенаправлять такой домен на новый
          сайт обычно бесполезно: Google уже передал вес.
        </li>
        <li>
          <strong>Захват темы (theme takeover)</strong> — раньше был блог про
          пиццу, последние два года — обзоры казино-бонусов. Профиль
          беклинков всё ещё «топовый», но контент — другой сайт.
        </li>
        <li>
          <strong>Паркинг всю жизнь</strong> — длинная история в CDX, но
          каждый снимок — однострочная заглушка. Ахрефс может показывать
          метрики, реальной аудитории не было.
        </li>
      </ul>
      <p>
        По умолчанию <strong>выключен</strong>: запросы к CDX медленные,
        нужен не каждой триаге.
      </p>

      <h2>Два слоя</h2>
      <p>Критерий работает в два уровня — нижний всегда, верхний опционально:</p>
      <ol>
        <li>
          <strong>CDX V1 (метаданные).</strong> Один запрос к{" "}
          <code>web.archive.org/cdx/search/cdx</code> возвращает список всех
          снимков: <code>timestamp</code>, <code>original</code> URL,{" "}
          <code>statuscode</code>, <code>mimetype</code>, <code>length</code>.
          Дёшево, всегда исполняется при включённом критерии.
        </li>
        <li>
          <strong>V2 (содержимое страниц).</strong> Когда включён флажок{" "}
          <em>«Sample snapshot pages»</em>, пикер выбирает N строк из CDX и
          для каждой скачивает архивный HTML через{" "}
          <code>web.archive.org/web/{"{ts}"}id_/{"{url}"}</code>. Парсер
          извлекает <code>&lt;title&gt;</code>, <code>h1</code>/<code>h2</code>/
          <code>h3</code>, первые 150 символов body, и атрибут{" "}
          <code>&lt;html lang&gt;</code>. Медленно — каждая страница ~1–3
          секунды под рейт-лимитом.
        </li>
      </ol>
      <p>
        Суффикс <code>id_</code> в URL снимка обязателен: без него Wayback
        вставляет в HTML свой тулбар и JS-обёртки, и парсер ловит мусор.
      </p>

      <h2>Настройки CDX V1</h2>
      <ul>
        <li>
          <strong>Limit</strong> (по умолчанию <code>100</code>, диапазон
          1–200). Сколько строк забирать из CDX. 100 уже даёт ИИ-судье
          максимум сигнала; верхняя планка в 200 защищает от того, чтобы
          случайно сконфигурировать батч-задачу, которая каскадно отвалится
          по таймауту. Повышать вручную имеет смысл только для одного
          конкретного домена, в чьей глубокой истории вы реально копаетесь.
        </li>
        <li>
          <strong>From year / To year</strong> — рамка по годам, обе пустые
          по умолчанию. На доменах с очень длинной историей полезно сузить
          до последних 5 лет — отсекает доисторический шум.
        </li>
        <li>
          <strong>Match type</strong>:
          <ul>
            <li>
              <code>host</code> — <em>рекомендуется по умолчанию</em>. Один
              хост без поддоменов. На свободном бэкенде CDX это самая
              быстрая форма; на практике даёт тот же сигнал, что{" "}
              <code>domain</code>, в ~95% случаев.
            </li>
            <li>
              <code>exact</code> — только точный URL.
            </li>
            <li>
              <code>prefix</code> — URL начинается с цели.
            </li>
            <li>
              <code>domain</code> — хост + все поддомены.{" "}
              <strong>Самая тяжёлая форма запроса.</strong> На батче из 20+
              доменов с <code>domain</code> CDX уходит в каскад
              ConnectTimeout'ов (инцидент 2026-05-07: 31 из 35 доменов в
              батче упали). Использовать только под одиночную глубокую триагу.
            </li>
          </ul>
        </li>
        <li>
          <strong>Collapse</strong> (по умолчанию <code>timestamp:6</code>).
          Параметр CDX <code>collapse=</code> схлопывает{" "}
          <strong>соседние</strong> строки с одинаковыми{" "}
          <em>префиксами поля</em>.
          <br />
          <strong>Важно, как именно работает «соседние».</strong> CDX
          сортирует ответ сначала по <code>urlkey</code>, затем по{" "}
          <code>timestamp</code>. То есть с <code>match_type=host</code>
          {" "}(по умолчанию) или <code>domain</code> ответ — это{" "}
          <em>группы по URL</em>: сначала все снимки <code>/</code>, потом
          все снимки <code>/about</code>, потом <code>/blog/post-1</code> и
          т.&nbsp;д. Collapse работает <strong>внутри каждой группы</strong>,
          не поверх всего ответа. Значит{" "}
          <code>collapse=timestamp:N</code> = «один снимок на гранулярность
          времени <em>на каждый URL</em>», а не «на весь сайт». Если у
          домена 50 распознанных URL за 10 лет —{" "}
          <code>collapse=timestamp:4</code> даст до 500 строк, а не 10.
          <br />
          <strong>Если вы хотите 1 снимок на год на весь сайт:</strong>
          {" "}переключите <code>match_type</code> на <code>exact</code>{" "}
          (или <code>prefix</code> от корня) — тогда в ответе один
          <code>urlkey</code>, и collapse действительно сводится к «одной
          точке на год для домена». С <code>host</code>/<code>domain</code>
          {" "}collapse всегда несёт мультипликатор от числа разных URL.
          <br />
          Wayback-таймстемп имеет формат <code>YYYYMMDDhhmmss</code> (14
          символов). <code>N</code> = сколько ведущих символов сравнивать:
          <ul>
            <li>
              <code>timestamp:4</code> — <strong>один снимок на год</strong>
              {" "}на URL. На сайте с одним URL даёт ~20 строк за 20 лет;
              на <code>host</code> с десятками подстраниц — заметно больше.
              Полезно как «скелет» истории под V2-сэмплирование.
            </li>
            <li>
              <code>timestamp:6</code> — <strong>один снимок на месяц</strong>
              {" "}на URL. По умолчанию. Хороший баланс сигнал/шум: не теряет
              событий (смена статуса, mimetype, длины), убирает плотный шум.
            </li>
            <li>
              <code>timestamp:8</code> — <strong>один снимок на день</strong>
              {" "}на URL. Нужно, когда вы триажите домен, где подозреваете
              конкретное событие в конкретный день (релонч, взлом, день
              редиректа) и хотите видеть переходы вокруг него с точностью
              до суток.
            </li>
            <li>
              <code>timestamp:10</code> — <strong>один снимок на час</strong>
              {" "}на URL. Редко полезно для drop-hunter'а: такая плотность
              нужна разве что криминалистике (смотреть час за часом, как
              менялся сайт в день инцидента).
            </li>
            <li>
              <code>timestamp:12</code> — один снимок на минуту на URL.
              Практически никогда не нужно: для drop-триажа это уже не
              схлопывание, а сохранение почти всех строк.
            </li>
            <li>
              <strong>Пусто</strong> (без <code>collapse=</code>) — без
              схлопывания, каждая строка как есть. Имеет смысл, когда вы
              целенаправленно смотрите на плотный паттерн (например, на
              сайте-паркинге Wayback крутил один и тот же URL 50 раз за
              день — вы хотите это видеть, чтобы ИИ-судья получил
              подтверждение «однотипная мёртвая страница»). Учтите, что{" "}
              <code>limit</code> при этом начнёт упираться раньше — вместо
              20 лет истории получите неделю на пределе в 100 строк.
            </li>
          </ul>
          Значения <code>timestamp:1</code>–<code>timestamp:3</code> формально
          валидны (схлопывание по десятилетию / двум первым цифрам года),
          но смысла для триажа не несут и здесь не перечислены.
          <br />
          <strong>Совет:</strong> если вы поднимаете <code>limit</code> до
          максимума (200) и всё равно упираетесь в потолок на длинном
          домене на <code>match_type=host</code> — есть два пути:
          переключить collapse на <code>timestamp:4</code> (получите больше
          лет в ответе, но всё ещё с per-URL-мультипликатором), или
          сузить <code>match_type</code> до <code>exact</code> (видите
          только корень сайта, но компактно: 1 строка / год).
          <br />
          <em>Дополнительно:</em> поле <code>collapse=</code> поддерживает
          и не-временны́е варианты — например, <code>digest</code>{" "}
          схлопывает соседние снимки с одинаковым SHA1 контента (умирающий
          паркинг → одна строка вместо сотни). В UI поле — свободный ввод,
          можно вписать руками; в формах батчей мы их не предлагаем,
          потому что для drop-триажа сочетания{" "}
          <code>match_type</code> + <code>timestamp:4</code>/
          <code>6</code>/<code>8</code> покрывают практически все
          сценарии.
        </li>
      </ul>

      <h2>Настройки V2 — сэмплирование страниц</h2>
      <p>
        Все четыре поля имеют смысл только когда{" "}
        <strong>Sample snapshot pages</strong> включён.
      </p>
      <ul>
        <li>
          <strong>Sample count</strong> (по умолчанию <code>6</code>,
          диапазон 1–15). Сколько архивных страниц скачивать на домен. Шесть
          штук на 20-летней истории ≈ один снимок раз в ~3 года при
          стратегии <em>even</em>. Каждая страница — ~1–3 секунды под
          рейт-лимитом, так что 6 сэмплов добавляют 10–20 секунд к домену.
        </li>
        <li>
          <strong>Sample strategy</strong>:
          <ul>
            <li>
              <strong>Even</strong> — квантильное распределение по таймлайну.
              Фильтрует строки до <code>status=200</code> +{" "}
              <code>html</code> mimetype (только эти несут полезный
              title-сигнал), берёт равномерно <em>N</em> точек. Если строк
              после фильтра меньше бюджета (например, домен большую часть
              жизни был на 301), фолбэк на весь набор — паркинг тоже
              сэмплируется, и ИИ видит консистентный паттерн редиректов.
              <br />
              <em>Когда брать:</em> по умолчанию. Когда вы ещё не знаете, где
              в истории интересные точки.
            </li>
            <li>
              <strong>Anchor</strong> — отбор вокруг CDX-аномалий. Всегда
              включает первый и последний снимок (bookends), плюс точки
              рядом со сменой статус-кода, mimetype, скачком длины ответа
              &gt; 5× и временным пробелом ≥ 2 года между соседними
              снимками. Если приоритетных якорей меньше бюджета — добивает
              даже-spaced строками из неотсэмплированных. Приоритеты (меньше
              = важнее): 0 bookend → 1 status → 2 mimetype → 3 length → 4
              gap.
              <br />
              <em>Когда брать:</em> когда уже подозреваете переход (хвост
              301, флип в PDF, релонч) и хотите плотнее покрыть границы, а
              не тратить пики на ровные участки.
            </li>
          </ul>
        </li>
        <li>
          <strong>URL path mode</strong>:
          <ul>
            <li>
              <strong>Mixed</strong> (по умолчанию) — для каждого
              выбранного снимка скачивается тот URL, на который указывает
              сама CDX-строка (<code>original</code>). Достаёт контент
              подстраниц.
            </li>
            <li>
              <strong>Root</strong> — всегда скачивается{" "}
              <code>https://{"{domain}"}/</code>. Сравнения «яблоки к
              яблокам», но пропускает сайты, у которых контент был только на
              подстраницах.
            </li>
          </ul>
        </li>
      </ul>

      <h2>Что ИИ-судья оценивает</h2>
      <p>
        Промпт <code>WAYBACK_PROMPT</code> взвешивает (в порядке важности):
      </p>
      <ol>
        <li>
          <strong>Хвост 301/302 в недавних снимках</strong> — сильнейший
          негативный сигнал. Любой паттерн свежих редиректов ⇒{" "}
          <code>low_quality</code> независимо от старой истории.
        </li>
        <li>
          <strong>Распределение статус-кодов в целом</strong> — здоровый
          сайт = большинство 200.
        </li>
        <li>
          <strong>Дата первого и последнего снимка</strong> — возраст и
          актуальность.
        </li>
        <li>
          <strong>Плотность снимков</strong> — регулярные краулы за много
          лет = Wayback считал сайт стоящим индексации.
        </li>
        <li>
          <strong>Эволюция URL-путей</strong> (только когда V2 выключен;
          самый слабый сигнал, V2 заменяет его прямым чтением title'ов).
        </li>
        <li>
          <strong>Mimetype-микс</strong> — преимущественно text/html =
          настоящий сайт.
        </li>
      </ol>
      <p>
        Confidence явно штрафуется, когда снимков меньше 30.
      </p>
      <p>
        <strong>Когда V2 включён</strong>, добавляются ключевые правила:
      </p>
      <ul>
        <li>
          Title + h1 сравниваются хронологически. Две разные темы ⇒
          подтверждённый takeover; обе темы цитируются в{" "}
          <code>red_flags</code> с приблизительными годами.
        </li>
        <li>
          Пустой title + пустые headings + не-200 на сэмпле = домен был
          недоступен / запаркован. Несколько подряд = сайт умер.
        </li>
        <li>
          <strong>3xx-снепшоты содержат поле <code>redirect_to</code></strong>{" "}
          с распакованным Location-хедером. Тело Apache-заглушки{" "}
          <em>не</em> парсится: оно не несёт информации, только засоряет
          промпт. Wayback переписывает Location в архивные URL-обёртки —
          провайдер их снимает регуляркой, ИИ и UI видят чистый
          конечный URL. Свежий редирект на конкурента или агрегатор —
          сильнейший возможный сигнал takeover'а.
        </li>
        <li>
          Шаблонные одно-слово title'ы («Home», «Index of /») на всех
          сэмплах = parking или templated, независимо от количества
          CDX-строк.
        </li>
      </ul>

      <h2>Кэш</h2>
      <p>
        Wayback участвует в общем механизме кэша — см.{" "}
        <Link href="/docs/cache">«Кэш»</Link>. Особенность одна:{" "}
        <strong>четыре V2-поля</strong> (<code>sample_pages</code>,{" "}
        <code>sample_count</code>, <code>sample_strategy</code>,{" "}
        <code>sample_path_mode</code>) попадают в хеш параметров{" "}
        <em>только когда <code>sample_pages = true</code></em>. Это значит:
      </p>
      <ul>
        <li>
          Включаете V2 на уже закэшированном домене → промах кэша, свежий
          сэмплинг. Это правильно — вы хотите данные.
        </li>
        <li>
          Выключаете V2 → продолжаете использовать старую V1-кэш-запись без
          повторного запроса.
        </li>
        <li>
          Меняете <code>sample_count</code> с 6 на 10 → промах кэша, новый
          сэмплинг.
        </li>
        <li>
          Кэш-хит наследует сэмплы бесплатно: новый CR копируется целиком,
          вместе с уже распарсенными страницами.
        </li>
      </ul>

      <h2>Wayback-only переоценка и Augmentation</h2>
      <p>
        Если вы перезапускаете домен только по Wayback (например, после
        правки промпта), старые колонки Ahrefs <em>не пропадают</em> — База
        и страница домена сшивают данные «по последнему свежему CR на
        критерий» через цепочку Run'ов. Подробнее — в{" "}
        <Link href="/docs/augmentation">«Augmentation chain»</Link> и{" "}
        <Link href="/docs/reanalyze">«Переоценка»</Link>.
      </p>

      <h2>Связь с Wayback Classify</h2>
      <p>
        <Link href="/docs/wayback-classify">Wayback Classify</Link> —
        отдельный критерий поверх этого. Он <em>не делает</em> новых
        HTTP-запросов: читает те же V2-сэмплы и определяет язык + тематику
        + категорию домена. При включении Classify автоматически
        активируются <code>enabled</code> и <code>sample_pages</code> у
        Wayback History — без сэмплов классифицировать нечего.
      </p>

      <h2>Производительность и стоимость</h2>
      <ul>
        <li>
          <strong>Один CDX-запрос</strong> — 1–30 секунд (на коротких
          историях быстро, на geocities-классе долго). Один HTTP-вызов вне
          зависимости от <code>limit</code>.
        </li>
        <li>
          <strong>V2 на домен при count=6</strong> — +10–20 секунд поверх
          CDX.
        </li>
        <li>
          <strong>V2 на домен при count=15</strong> — +25–45 секунд.
        </li>
        <li>
          <strong>Батч 35 доменов, только V1</strong> — ~3–6 минут под
          рейт-лимитом.
        </li>
        <li>
          <strong>Батч 35 доменов, V2 count=6</strong> — ~10–15 минут.
        </li>
      </ul>
      <p>
        Сам CDX и архивные страницы — бесплатны (Internet Archive). Платный
        ресурс — только время и терпение upstream'а. Wayback Classify уже
        тратит ИИ-токены (два вызова на домен).
      </p>

      <h2>Типичные проблемы</h2>
      <ul>
        <li>
          <strong>«network error after 4 attempts» на одном домене</strong>{" "}
          — CDX таймаут. Понизьте <code>limit</code>, сузьте годовой
          диапазон или переключитесь на <code>match_type=host</code>.
          Сообщение об ошибке уже подсказывает это.
        </li>
        <li>
          <strong>Каскадные ConnectTimeout'ы на батче</strong> — почти
          всегда <code>match_type=domain</code> на длинной истории.
          Переключите на <code>host</code>.
        </li>
        <li>
          <strong>Все V2-сэмплы вернули пустой title</strong> — это
          исторический баг, починен 2026-05-07 (<code>head</code> ошибочно
          скипался целиком вместе с title-тегом). Перезапустите критерий,
          если в данных встречаются такие старые записи.
        </li>
        <li>
          <strong>3xx-сэмплы содержат только текст Apache-заглушки</strong>{" "}
          — поведение до фикса <code>redirect_to</code>. Перезапустите —
          новый провайдер кладёт распакованный Location в структурное поле.
        </li>
      </ul>
    </div>
  );
}
