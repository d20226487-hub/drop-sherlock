import Link from "next/link";

export const metadata = { title: "Резервные копии — Drop Sherlock" };

export default function BackupsDoc() {
  return (
    <div className="docs-content">
      <h1>Резервные копии</h1>
      <p>
        Drop Sherlock хранит все данные в одном SQLite-файле{" "}
        (<code>data/drop_sherlock.db</code>). Резервная копия — это копия
        этого файла. Делается локально (в той же системе) и опционально
        отправляется на S3-совместимое хранилище.
      </p>

      <h2>Где настраивается</h2>
      <p>
        <Link href="/docs/settings">Настройки</Link> → «Other» → «Backups».
        Здесь:
      </p>
      <ul>
        <li>
          <strong>Расписание</strong> — частота создания локальных
          бэкапов. Дефолт — ежедневно.
        </li>
        <li>
          <strong>Ротация</strong> — сколько последних копий хранить
          локально. Старые автоматически удаляются.
        </li>
        <li>
          <strong>Локальный путь</strong> — папка, куда складываются
          бэкапы (обычно <code>data/backups/</code>).
        </li>
        <li>
          <strong>S3-выгрузка</strong> — включается отдельно. Поля:
          endpoint, bucket, access_key, secret_key, опциональный prefix.
          Работает с S3-совместимыми сервисами (AWS S3, MinIO, Wasabi,
          Backblaze B2 и&nbsp;т.&nbsp;д.).
        </li>
      </ul>

      <h2>Как создаётся бэкап</h2>
      <p>
        Drop Sherlock использует SQLite-команду{" "}
        <code>VACUUM INTO</code> — это создаёт чистую копию базы в
        отдельный файл, не блокируя текущую запись. На большой БД может
        занять секунды; на нашем масштабе незаметно.
      </p>
      <p>Каждый файл бэкапа имеет имя вида:</p>
      <pre><code>drop_sherlock-2026-05-11T03-00-00Z.db</code></pre>
      <p>
        Сразу после создания файл (опционально) загружается в S3 через
        boto3. Если S3 недоступен, локальная копия всё равно остаётся.
      </p>

      <h2>Что входит в бэкап</h2>
      <p>
        Всё: Очередь, задачи, запуски, RunDomain'ы, CR-строки с сырыми
        Ahrefs JSON, ИИ-вердикты, заметки, пины, ключи провайдеров
        (зашифрованные Fernet), промпты, scoring config.
      </p>
      <p>
        Что <em>не</em> входит: ключ шифрования (
        <code>FERNET_KEYS</code> в <code>.env</code> или файл{" "}
        <code>/data/.fernet_key</code>), пароль basicauth,{" "}
        <code>Caddyfile</code>. Это файлы вне SQLite — храните их
        отдельно. Для полного восстановления нужны и БД,{" "}
        <em>и</em> ключ шифрования (иначе провайдерские ключи и
        S3-доступы нечитаемы).
      </p>

      <div className="callout callout-warn docs-content">
        <p>
          <strong>Ключевой момент threat-model:</strong> S3-бэкап
          выгружает только <code>drop_sherlock.db</code>. Файл{" "}
          <code>/data/.fernet_key</code> в облако не уходит. Утечка
          бэкапа в S3 → атакующий получает зашифрованную БД, но без
          ключа провайдерские ключи остаются нечитаемы. Это и есть
          смысл шифрования. Не складывайте ключ в тот же бакет.
        </p>
      </div>

      <h2>Восстановление</h2>
      <ol>
        <li>
          Остановите контейнер api (
          <code>docker compose stop api</code>).
        </li>
        <li>
          Замените <code>data/drop_sherlock.db</code> файлом из бэкапа.
        </li>
        <li>
          Поднимите api (<code>docker compose up -d api</code>). При
          старте Drop Sherlock прогонит миграции — если бэкап от старой
          версии, схема догонится.
        </li>
      </ol>
      <p>
        Если у вас включена S3-выгрузка, восстановление с удалённой
        копии — это просто скачать файл и положить вместо
        <code>data/drop_sherlock.db</code>.
      </p>

      <h2>Ручной запуск бэкапа</h2>
      <p>
        Помимо расписания, бэкап можно создать вручную из{" "}
        <Link href="/docs/settings">Настроек</Link> → «Other» →
        «Backups» — кнопка «Run backup now». Полезно перед рискованной
        операцией (массовое удаление, миграция).
      </p>

      <h2>Безопасность</h2>
      <p>
        Бэкап содержит зашифрованные ключи провайдеров (Fernet). Но
        Fernet работает с симметричным ключом — лежит либо в{" "}
        <code>FERNET_KEYS</code> в <code>.env</code>, либо в файле{" "}
        <code>/data/.fernet_key</code>. Кто получит и БД, и ключ —
        расшифрует. Поэтому:
      </p>
      <ul>
        <li>
          Храните <code>.env</code> отдельно от бэкапов БД.
        </li>
        <li>
          Если используете режим «ключ в файле» — следите, что{" "}
          <code>/data/.fernet_key</code> не попал случайно в общий
          rsync со снапшотами БД.
        </li>
        <li>
          S3-bucket — приватный, с отдельными credentials.
        </li>
        <li>
          Ротация ключа: сгенерируйте новый ключ, поставьте его{" "}
          <em>первым</em> в <code>FERNET_KEYS=новый,старый</code>,
          перезапустите контейнер, пересохраните каждый провайдерский
          ключ через UI (новые записи зашифруются новым ключом).
          Когда все записи переписаны — удалите старый ключ из списка.
        </li>
      </ul>

      <h2>VACUUM и обслуживание БД</h2>
      <p>
        Добавлено 2026-05-14. SQLite не возвращает место на диск
        автоматически после удалений — освободившиеся страницы помечены
        как свободные, но размер файла не уменьшается. После того, как
        Drop Sherlock начал активно удалять (ретенция{" "}
        <code>availability_checks</code>, «Удалить всё отфильтрованное»
        в Очереди, снимки/восстановления Backlog при бане), без VACUUM
        файл рос бы постоянно.
      </p>
      <p>
        Раз в месяц (1-е число, 03:30 UTC) Drop Sherlock запускает{" "}
        <code>VACUUM</code> через APScheduler. Это перестраивает файл
        БД с нуля, копируя живые данные во временный файл и атомарно
        заменяя оригинал. Транзакционно — никакая потеря данных
        невозможна. Бэкапы не трогаются.
      </p>
      <p>Защитные проверки перед запуском:</p>
      <ul>
        <li>
          <strong>Место на диске.</strong> VACUUM пишет временную
          копию рядом с БД, поэтому нужно свободно ≥ 2× размера БД.
          Если меньше — VACUUM пропускается с лог-предупреждением; не
          трогает оригинал.
        </li>
        <li>
          <strong>Перекрытие с бэкапом.</strong> Backup и VACUUM
          делят один <code>MAINTENANCE_LOCK</code>: если бэкап уже
          идёт, VACUUM пропускает запуск; если VACUUM идёт, бэкап
          ждёт следующего цикла. Обе операции одновременно не
          запустятся.
        </li>
        <li>
          <strong>Тумблер в Настройках.</strong> Endpoint{" "}
          <code>PUT /settings/db-maintenance/vacuum-enabled</code>{" "}
          выключает / включает ежемесячный запуск. По умолчанию
          включено.
        </li>
      </ul>
      <p>
        Ручной триггер:{" "}
        <code>POST /settings/db-maintenance/vacuum-now</code>. Применяет
        те же защитные проверки и возвращает{" "}
        <code>{`{status, bytes_before, bytes_after, reclaimed, free_before}`}</code>{" "}
        — удобно убедиться, что место реально освободилось.
      </p>

      <h2>Ретенция «Недавних проверок»</h2>
      <p>
        Добавлено 2026-05-14. Таблица <code>availability_checks</code>{" "}
        (история каждого ответа провайдеров каскада доступности)
        раньше росла без ограничений. Теперь — ежедневная очистка с
        двумя совмещающимися лимитами:
      </p>
      <ul>
        <li>
          <strong>Возраст</strong> — удалить строки старше N дней
          (по умолчанию 30; 0 = не чистить по возрасту).
        </li>
        <li>
          <strong>На домен</strong> — после возрастной зачистки, если
          у домена осталось &gt; M записей, оставить только M самых
          свежих (по умолчанию 20; 0 = без лимита).
        </li>
      </ul>
      <p>
        Управляется в <Link href="/docs/settings">Настройках</Link>{" "}
        → «Domain availability» → секция «Recent-checks retention».
        Дополнительно запускается один раз при старте контейнера —
        чтобы после долгой остановки догнать накопленное.
      </p>
    </div>
  );
}
