import Link from "next/link";

export const metadata = { title: "Закрепление — Drop Sherlock" };

export default function PinningDoc() {
  return (
    <div className="docs-content">
      <h1>Закрепление (Pinning)</h1>
      <p>
        В Drop Sherlock есть два независимых уровня пинов: per-(Job,
        критерий) — основной, через который Страница{" "}
        <Link href="/docs/database">Базы</Link> собирает строки; и
        per-Job Run pin — отдельный, влияет только на сводные плашки
        вверху страницы Задачи. Старая модель с тремя уровнями (когда
        существовал ещё RunDomain pin) была переработана 2026-05-12 и
        окончательно подчищена 2026-05-14.
      </p>

      <h2>Два уровня</h2>
      <table>
        <thead>
          <tr>
            <th>Уровень</th>
            <th>Что закрепляет</th>
            <th>На что влияет</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <strong>job_criterion_pin</strong>
            </td>
            <td>Один Run на каждый критерий внутри Job</td>
            <td>
              <Link href="/docs/database">Базу</Link>: какой Run даёт
              данные для каждого критерия каждого домена этой Задачи
            </td>
          </tr>
          <tr>
            <td>
              <strong>Run.is_pinned</strong>
            </td>
            <td>Один конкретный Run (один на Job)</td>
            <td>
              <Link href="/docs/jobs">Страницу задачи</Link>: какой
              запуск даёт цифры для сводных плашек L/M/H в шапке.
              Полностью независим от per-criterion пинов.
            </td>
          </tr>
        </tbody>
      </table>

      <h2>Пины по критериям (per-(Job, критерий)) — основной уровень</h2>
      <p>
        Это «истина» для строк Базы. Один пин — запись «для этой Задачи,
        критерий C берётся из Run R». В пределах одной Задачи каждый
        критерий{" "}
        (<code>backlinks</code> / <code>refdomains</code> /{" "}
        <code>anchors</code> / <code>keywords</code> /{" "}
        <code>wayback</code> / <code>wayback_classify</code>) может
        указывать на свой собственный Run. Страница Базы для каждого
        домена аккуратно сшивает по-критериально пинённые данные в одну
        строку — например, B/D/A/K от одного запуска, W/C от другого.
      </p>

      <p>Инварианты:</p>
      <ul>
        <li>Не больше одного пина на пару (Job, критерий).</li>
        <li>
          Закрепить можно только <code>done</code>-запуск (тот же
          принцип, что у Run.is_pinned).
        </li>
        <li>
          Run должен принадлежать той же Задаче, что и пин (проверяется
          на бэкенде).
        </li>
        <li>
          Если у пин-Run нет CR-данных по своему критерию для
          конкретного домена — ячейка остаётся пустой (никакого
          fallback на «свежий» Run). Это намеренно: пин — это явная
          фиксация, и заменить её молчаливым подменником было бы
          враньём пользователю.
        </li>
        <li>
          Если домен живёт в нескольких Задачах и у каждой свой пин,
          Страница Базы для этого домена берёт данные из{" "}
          <em>самой свежей по <code>updated_at</code></em> пин-записи.
          Это «cross-job stitching»; на странице домена видна плашка
          «Stitched from Run #N (Job J)», чтобы было видно, что
          происходит.
        </li>
      </ul>

      <p>Где закрепляется:</p>
      <ul>
        <li>
          <strong>Основной интерфейс:</strong> на{" "}
          <Link href="/docs/run">странице запуска</Link>, секция{" "}
          <strong>«Per-criterion pins»</strong> — свёрнутая по
          умолчанию панель под таблицей доменов. Шесть карточек, по
          одной на критерий. Клик: закрепить на этот Run / открепить.
          В бейдже в шапке панели всегда видно «N/6 закреплено здесь».
        </li>
        <li>
          <strong>Bulk-операция:</strong> кнопка{" "}
          <strong>«Pin all populated»</strong> в той же панели —
          закрепляет одним кликом все критерии, по которым у этого
          Run есть данные. Заменила удалённую кнопку «Pin entire run».
        </li>
        <li>
          <strong>Read-only обзор:</strong> на{" "}
          <Link href="/docs/jobs">странице Задачи</Link> есть виджет
          «Per-criterion pins» — кросс-обзор «B ← Run #77, W ← Run #61,
          …». Только для чтения; изменения делаются со страницы Run.
        </li>
      </ul>

      <h3>Частичный балл</h3>
      <p>
        Если в Задаче закреплено не всё (например, только W и K, а
        B/D/A/K — нет), Страница Базы для этого домена покажет один из
        двух бейджей рядом с финалом:
      </p>
      <ul>
        <li>
          <strong>Subset</strong> (янтарный) — балл рассчитан по
          подмножеству критериев. В подсказке перечислены{" "}
          <em>недостающие</em> критерии (например «missing B, D»),
          чтобы было видно, что докрутить.
        </li>
        <li>
          <strong>Failed</strong> (красный) — у одного из включённых
          критериев на этапе AI-синтеза была ошибка. Эта плашка про
          реальный сбой, а не про неполноту пинов.
        </li>
      </ul>
      <p>
        Раньше оба случая показывались одной плашкой «partial» —
        раздельная версия появилась 2026-05-14 после того, как стало
        неочевидно, что именно показано.
      </p>

      <h2>Run pin (per-Job) — для плашек сводки</h2>
      <p>
        У одной Задачи может быть несколько Runs. Какой из них
        представляет «состояние» Задачи в сводных плашках L/M/H в шапке?
        Без пина — самый свежий (<code>max(Run.id)</code>). С пином —
        закреплённый.
      </p>
      <p>
        Зачем: вы сделали полный прогон 200 доменов, а затем частичный
        rerun только 10 доменов под A/B-тест промпта. По умолчанию
        плашки покажут статистику по этим 10 доменам (последний run) —
        что вводит в заблуждение. Закрепляете полный 200-доменный run
        — плашки возвращаются к полной картине.
      </p>
      <p>Инварианты:</p>
      <ul>
        <li>Не больше одного пина на Задачу.</li>
        <li>
          Закрепить можно только <code>done</code>-запуск.
        </li>
        <li>
          Закрепление другого запуска <em>заменяет</em> старый пин (без
          подтверждения — это «переключатель»).
        </li>
        <li>
          Полностью независим от per-criterion пинов. Run.is_pinned
          влияет только на L/M/H плашки; данные Базы он не трогает.
        </li>
      </ul>
      <p>Где закрепляется:</p>
      <ul>
        <li>
          На <Link href="/docs/jobs">странице Задачи</Link>, в строке
          каждого Run — кнопка <strong>«Pin»</strong> (для{" "}
          <code>done</code>; иначе кнопка скрыта).
        </li>
      </ul>

      <h2>Что было удалено / переработано</h2>
      <p>
        Если вы помните старое поведение — вот что поменялось:
      </p>
      <ul>
        <li>
          <strong>«Pin entire run»</strong> — кнопка удалена 2026-05-12.
          Её делал «Pin all populated» в панели Per-criterion pins на
          странице Run.
        </li>
        <li>
          <strong>Per-row «Pin» колонка на странице Run</strong> —
          удалена в том же релизе. Закрепление идёт через панель
          Per-criterion pins под таблицей, не по строке.
        </li>
        <li>
          <strong>Селектор «Pin» в Базе</strong> (выбор из списка
          RunDomain) — удалён 2026-05-14. Был мёртвый код ещё с
          реструктуризации Базы; пины теперь — атрибут Задачи, не
          атрибут отдельного RunDomain.
        </li>
        <li>
          <strong>Колонка <code>RunDomain.is_pinned</code> в БД</strong>{" "}
          — оставлена для read-back compat (миграция при старте
          разворачивает старые значения в job_criterion_pin), но
          endpoint-ы её больше не записывают. Со временем эта колонка
          станет полностью архивной.
        </li>
      </ul>

      <h2>Совместимость со старыми пинами</h2>
      <p>
        При первом запуске после обновления уже существующие{" "}
        <code>Run.is_pinned</code> и <code>RunDomain.is_pinned</code>{" "}
        автоматически разворачиваются в пины по критериям — по одной
        записи на каждый критерий, по которому у пин-Run есть данные.
        Если пин-Run не содержал какого-то критерия, этот критерий
        останется без пина (no-fallback). Чтобы вернуть данные —
        закрепите критерий из соответствующего Run на странице того
        Run.
      </p>

      <h2>Сводная шпаргалка</h2>
      <table>
        <thead>
          <tr>
            <th>Хочу...</th>
            <th>Какой пин</th>
            <th>Где</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Зафиксировать критерий на конкретный Run в Задаче</td>
            <td>job_criterion_pin</td>
            <td>
              Run page → Per-criterion pins → клик по критерию
            </td>
          </tr>
          <tr>
            <td>Закрепить все критерии этого запуска одним кликом</td>
            <td>job_criterion_pin (массово)</td>
            <td>
              Run page → Per-criterion pins → «Pin all populated»
            </td>
          </tr>
          <tr>
            <td>
              Посмотреть, какой Run даёт какой критерий в этой Задаче
            </td>
            <td>—</td>
            <td>
              Job page → виджет «Per-criterion pins» (read-only)
            </td>
          </tr>
          <tr>
            <td>Снять пин с конкретного критерия</td>
            <td>job_criterion_pin (delete)</td>
            <td>
              Run page → Per-criterion pins → повторный клик по
              критерию
            </td>
          </tr>
          <tr>
            <td>Чтобы плашки Задачи считались по конкретному прогону</td>
            <td>Run.is_pinned</td>
            <td>Job page → строка запуска → Pin</td>
          </tr>
          <tr>
            <td>Откатить — плашки Задачи вернутся к latest</td>
            <td>Unpin Run</td>
            <td>Job page → строка запуска → Unpin</td>
          </tr>
        </tbody>
      </table>

      <div className="callout callout-info docs-content">
        <p>
          <strong>На что обратить внимание:</strong> «Pin all populated»
          закрепляет на этот Run каждый критерий, по которому в нём
          есть данные — включая информационные (Wayback, wayback_classify)
          с весом 0 в Настройках. Если этого не нужно (например,
          вы хотите оставить W из другого Run), фиксируйте критерии
          по отдельности.
        </p>
      </div>
    </div>
  );
}
