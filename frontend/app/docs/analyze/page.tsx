import Link from "next/link";

export const metadata = { title: "Анализ — Drop Sherlock" };

export default function AnalyzeDoc() {
  return (
    <div className="docs-content">
      <h1>Анализ</h1>
      <p>
        Страница «Анализ» — это форма запуска. Вы указываете список
        доменов, набор критериев, ИИ-провайдера и нажимаете
        «Запустить» — Drop Sherlock создаёт{" "}
        <strong>Задачу</strong> (Job) с одним <strong>Запуском</strong>{" "}
        (Run) и начинает сбор данных + ИИ-вердиктов.
      </p>

      <h2>Домены</h2>
      <p>
        Поле «Domains» — список через перевод строки или запятую. Drop
        Sherlock нормализует домены: убирает <code>https://</code>,{" "}
        <code>www.</code>, пути, дедуплицирует.
      </p>
      <p>
        Список можно ввести вручную или попасть на страницу{" "}
        <em>с уже заполненным</em> полем:
      </p>
      <ul>
        <li>
          из <Link href="/docs/backlog">Очереди</Link> через «Отправить в
          Анализ»;
        </li>
        <li>
          из <Link href="/docs/database">Базы</Link> через «Analyze
          selected» — там автоматически проставляется флаг{" "}
          <em>cross_job_cache</em> (см. ниже).
        </li>
      </ul>

      <h2>Критерии</h2>
      <p>
        Шесть критериев, каждый можно выключить. Подробный разбор каждого
        Ahrefs-критерия (какие поля приходят, что ИИ ищет, на что обращать
        внимание) — в статье{" "}
        <Link href="/docs/ahrefs-criteria">«Ahrefs-критерии»</Link>.
      </p>
      <ul>
        <li>
          <strong>Backlinks</strong> (B) — Ahrefs Site Explorer{" "}
          <em>all-backlinks</em>. Оценивает качество входящих ссылок.
        </li>
        <li>
          <strong>Refdomains</strong> (D) — Ahrefs{" "}
          <em>refdomains</em>. Уникальные домены-доноры и их характеристики.
        </li>
        <li>
          <strong>Anchors</strong> (A) — Ahrefs <em>anchors</em>. Распределение
          анкоров; ищет over-optimization и спам.
        </li>
        <li>
          <strong>Keywords</strong> (K) — Ahrefs{" "}
          <em>organic-keywords</em>. Какие ключи и сколько трафика они дают.
        </li>
        <li>
          <strong>Wayback</strong> (W) — Wayback Machine CDX + V2 sampling.
          История домена: тематический дрифт, редиректы, периоды простоя.
        </li>
        <li>
          <strong>Wayback Classify</strong> (C) — определяет язык и
          тематику из архивных снепшотов, затем сопоставляет с вашими
          категориями (см.{" "}
          <Link href="/docs/wayback-classify">отдельную статью</Link>).
        </li>
      </ul>
      <p>
        У каждого Ahrefs-критерия можно настроить сортировку (по каким
        полям API возвращает строки). Это влияет на то, какие именно строки
        попадут под лимит при усечении больших ответов. На странице
        Анализа есть «Live preview» — справа сразу видно, какой URL будет
        запрошен у Ahrefs и какие поля в нём.
      </p>

      <h2>ИИ-провайдер</h2>
      <p>
        Поле «Provider» и «Model». Дефолт берётся из{" "}
        <Link href="/docs/settings">Настроек</Link> → «API» (там лежат
        ключи и список включённых провайдеров). На странице Анализа
        выбор переопределяется — каждый запуск имеет собственную пару
        provider/model.
      </p>
      <p>
        Подробнее про подбор модели и стоимость — в статье{" "}
        <Link href="/docs/ai">«ИИ: провайдеры, модели, стоимость»</Link>.
      </p>

      <h2>Кэш</h2>
      <p>
        Два независимых флажка:
      </p>
      <ul>
        <li>
          <strong>use_cache</strong> — если в этой <em>же</em> Задаче уже
          был запуск, и параметры (URL Ahrefs, промпт, модель) совпадают,
          переиспользует данные и вердикты без новых запросов. По
          умолчанию <em>включён</em>. Виден только при rerun (для свежего
          запуска переиспользовать нечего).
        </li>
        <li>
          <strong>cross_job_cache</strong> — расширяет поиск кэша на{" "}
          <em>все</em> прошлые задачи. По умолчанию выключен. Автоматически
          включается, когда вы пришли на Анализ через «Analyze selected» из
          Базы — там предполагается, что вы хотите переиспользовать всё
          уже собранное.
        </li>
      </ul>
      <p>
        Полная статья — <Link href="/docs/cache">«Кэш»</Link>.
      </p>

      <h2>Язык</h2>
      <p>
        Язык интерфейса (EN/RU из переключателя в шапке) передаётся в
        запуск автоматически. На RU-запусках ко всем системным промптам
        добавляется директива «отвечай по-русски» (текст этой директивы
        редактируется в <Link href="/docs/brain">Brain</Link>).
      </p>

      <h2>Подтверждение и запуск</h2>
      <p>
        Кнопка «Submit» создаёт Задачу и сразу редиректит на страницу{" "}
        Запуска. С этой секунды идут параллельные процессы:
      </p>
      <ol>
        <li>Drop Sherlock последовательно ходит в Ahrefs за данными.</li>
        <li>
          По мере получения данных запускает ИИ-судью по каждому критерию.
        </li>
        <li>
          Когда все критерии готовы — считает детерминированный итоговый
          балл и зовёт ИИ за нарративом final assessment (summary +
          recommendation).
        </li>
      </ol>
      <p>
        Прогресс виден на <Link href="/docs/run">странице Запуска</Link>.
      </p>

      <h2>Rerun</h2>
      <p>
        В Задаче со страницы Job есть кнопка{" "}
        <strong>«Перезапустить с новыми критериями»</strong>. Это создаёт
        новый Run внутри той же Job; форма Анализа открывается с
        предзаполненным списком и предыдущими настройками. Удобно для:
      </p>
      <ul>
        <li>
          A/B-тестов промптов или моделей (см.{" "}
          <Link href="/docs/workflow">«Рабочий процесс»</Link>, раздел 5).
        </li>
        <li>
          Повторного прогона после правки порогов в Brain.
        </li>
        <li>
          Доуточнения: оставляете один критерий (например,{" "}
          <code>wayback_classify</code>), остальные снимаете — экономите
          юниты и время.
        </li>
      </ul>
      <p>
        После завершения runs сравнивайте их через кнопку{" "}
        <strong>«Сравнить запуски»</strong> на странице Задачи — там видна
        матрица «домен × запуск → bucket». Если решили, какой run считаете
        эталонным, нажмите <strong>«Закрепить запуск»</strong> у нужной
        строки (см. <Link href="/docs/pinning">«Закрепление»</Link>).
      </p>
    </div>
  );
}
