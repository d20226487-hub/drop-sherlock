import Link from "next/link";

export const metadata = { title: "Ban List — Drop Sherlock" };

export default function BanListDoc() {
  return (
    <div className="docs-content">
      <h1>Ban List (бан-лист доменов)</h1>
      <p>
        Постоянный фильтр «никогда больше не анализировать этот домен».
        В отличие от статуса <code>discarded</code> в{" "}
        <Link href="/docs/backlog">Очереди</Link> (мягкая пометка для
        одного решения), бан — жёсткий, рекурсивный, применяется на
        каждой точке ингеста: импорт CSV в Backlog, Order/Discard на{" "}
        <Link href="/docs/database">Базе</Link>, каскад проверки
        доступности, отправка в Анализ.
      </p>

      <h2>Как открыть</h2>
      <p>
        Меню «База» в шапке → пункт «Ban List». Прямой адрес —{" "}
        <code>/banlist</code>.
      </p>

      <h2>Что показывает</h2>
      <ul>
        <li>
          <strong>Domain</strong> — нормализованная строка домена
          (lowercase, без схемы и пути).
        </li>
        <li>
          <strong>Note</strong> — свободный текст «почему забанили»
          (можно оставить пустым).
        </li>
        <li>
          <strong>Banned at</strong> — когда добавили в бан-лист.
        </li>
        <li>
          <strong>Действие</strong> — кнопка «Unban».
        </li>
      </ul>
      <p>
        Пагинация серверная — Drop Sherlock не загружает весь бан-лист
        в браузер. Поиск (по домену или примечанию) дебаунсится на
        250 мс и тоже идёт серверно. Pagination и search-поле работают
        вместе: смена поиска возвращает на первую страницу.
      </p>

      <h2>Как добавить домены в бан-лист</h2>
      <ul>
        <li>
          <strong>CSV-импорт</strong> на странице Ban List: вставьте
          одно поле на строку (или две колонки <code>domain, note</code>);
          empty-line и <code>#</code>-комментарии пропускаются.
          Идемпотентный — уже забаненные домены попадут в счётчик
          «already banned», не сломают импорт.{" "}
          <strong>Чанковый коммит:</strong> импорт коммитит каждые 500
          строк, чтобы 5-тысячный импорт не держал write-lock на
          секунды и не мешал параллельным анализам.
        </li>
        <li>
          <strong>Bulk Ban из Базы</strong> — на странице{" "}
          <Link href="/docs/database">Базы</Link> отметьте строки →
          кнопка «Ban N» в красной плашке.
        </li>
      </ul>

      <h2>Что происходит при бане</h2>
      <p>Симметричный ban/unban (locked 2026-05-14):</p>
      <ol>
        <li>
          Если у домена есть строка в Очереди — Drop Sherlock делает
          её JSON-снимок (status / registrar / expiration_date /
          comments / desired_price / max_price / created_at /
          updated_at) и пишет на бан-запись (колонка{" "}
          <code>backlog_snapshot_json</code>).
        </li>
        <li>
          Строка Backlog удаляется. На странице Очереди она
          перестаёт быть видна.
        </li>
        <li>
          Все будущие ингесты этого домена молча отклоняются (в
          импорте видно в счётчике «skipped banned»; при отправке в
          Анализ — структурная ошибка{" "}
          <code>code: all_banned</code> с локализованным сообщением).
        </li>
      </ol>
      <p>
        Если у домена строки в Очереди не было — снимок остаётся
        пустым, и при unban восстанавливать будет нечего.
      </p>

      <h2>Unban (восстановление)</h2>
      <p>
        Кнопка <strong>«Unban»</strong> в строке списка снимает
        конкретный домен; в bulk-плашке вверху — <strong>«Unban N
        selected»</strong> для отмеченных. Что происходит:
      </p>
      <ol>
        <li>
          Drop Sherlock читает <code>backlog_snapshot_json</code> с
          бан-записи. Если он не пустой и в Очереди до сих пор нет
          строки с этим доменом — пересоздаёт её.
        </li>
        <li>
          <strong>Восстановленная строка получает статус{" "}
          <code>banned</code></strong> (а не оригинальный, который
          был на момент бана). Сделано намеренно, чтобы её было видно
          через фильтр «Status = Banned» в Очереди и пользователь
          явно решал, что с ней делать дальше. Все остальные поля
          (регистратор, дата истечения, комментарии, цены) восстанавливаются
          один в один.
        </li>
        <li>
          Сама бан-запись удаляется. Новый ингест домена снова
          разрешён.
        </li>
      </ol>
      <div className="callout callout-info docs-content">
        <p>
          <strong>На что обратить внимание:</strong> bulk-unban делает
          один IN-list pre-load существующих Backlog-строк (вместо
          N+1 SELECT-ов) — десятки/сотни одновременных восстановлений
          обрабатываются за один проход. Для больших наборов это
          ощутимо быстрее, чем кликать по одному.
        </p>
      </div>

      <h2>Ошибка «all submitted domains are banned»</h2>
      <p>
        Если вы отправляете в Анализ только забаненные домены, бэкенд
        возвращает структурный ответ:
      </p>
      <pre>
        <code>{`{
  "detail": {
    "code": "all_banned",
    "count": 12,
    "sample": ["acme.kz", "test.com", ...],   // максимум 10
    "truncated": true                          // если count > 10
  }
}`}</code>
      </pre>
      <p>
        Frontend ловит этот ответ и показывает локализованное сообщение
        с укороченным списком вместо «голого» JSON. Раньше при сотнях
        бан-доменов сюда летела гигантская строка со всеми именами —
        теперь max 10 + «… (всего N)».
      </p>
    </div>
  );
}
