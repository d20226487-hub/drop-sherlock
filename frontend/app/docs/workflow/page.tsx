import Link from "next/link";

export const metadata = { title: "Рабочий процесс — Drop Sherlock" };

export default function WorkflowDoc() {
  return (
    <div className="docs-content">
      <h1>Рабочий процесс drop-hunter</h1>
      <p>
        Это сквозная статья: от первого CSV со списком освобождающихся
        доменов до момента, когда вы отмечаете купленный домен. Остальные
        разделы документации — это справочники по конкретным страницам;
        здесь же показано, как они соединяются в одну воронку.
      </p>
      <p>
        Допустим, ваша задача — найти 5–10 хороших дропов в неделю под
        линкбилдинг: чтобы у домена была реальная история, не было
        тематических подмен на казино и порно, остались живые анкоры и
        ключи. Drop Sherlock проводит первоначальный отсев и помогает не
        тратить ручное время на 90% мусора.
      </p>

      <h2>0. Откуда берутся домены</h2>
      <p>
        Сам инструмент не парсит списки дропов — это вы делаете снаружи:
        регистраторы (GoDaddy Auctions, Dynadot, Namecheap Market), биржи
        (Sav.com, Drop.com.ua, Expireddomains.net), внутренние парсеры
        backorder-сервисов. Любой формат, где есть колонка домена и
        желательно дата истечения — сгодится.
      </p>
      <p>
        Drop Sherlock не делает <strong>backorder</strong> — он только
        анализирует и помогает принять решение. Сам заказ вы ставите там, где
        обычно: на аукционе, у backorder-провайдера, ручной покупкой.
      </p>

      <h2>1. Загрузка в Очередь</h2>
      <p>
        Откройте{" "}
        <Link href="/docs/backlog">страницу «Очередь» (Backlog)</Link> и
        нажмите <strong>Импорт CSV</strong>. Минимально нужен один столбец с
        доменом; полезно ещё иметь дату истечения, регистратора и любые
        ваши заметки (внутренний приоритет, источник списка).
      </p>
      <p>
        Импорт дедуплицируется: если домен уже есть в очереди, повторная
        загрузка не создаёт второй строки. Это позволяет каждый день лить
        свежий выгруз с биржи поверх существующего — старые данные не
        затрутся.
      </p>
      <p>
        После импорта у всех новых строк статус <code>Backlog</code> —
        «сырые, нетриажены».
      </p>

      <div className="callout callout-info docs-content">
        <p>
          <strong>Подсказка:</strong> если в источнике уже есть цены или
          ваш приоритет, вы можете отредактировать поля <code>Желаемая&nbsp;$</code>{" "}
          и <code>Макс&nbsp;$</code> прямо в таблице Очереди — клик по
          ячейке открывает inline-редактор. То же с полем «Комментарии».
        </p>
      </div>

      <h2>2. Отсев в Очереди</h2>
      <p>
        Вряд ли вы будете анализировать через Ahrefs все 5000 строк после
        одной выгрузки — это дорого по юнитам и долго. Фильтры на странице
        Очереди нужны именно для предварительного отсева, до анализа:
      </p>
      <ul>
        <li>
          <strong>По регистратору</strong> — отрезать провайдеров, с
          которыми вы не работаете.
        </li>
        <li>
          <strong>По дате истечения</strong> — фокус на тех, которые
          дропают в ближайшие дни.
        </li>
        <li>
          <strong>По статусу</strong> — на этом этапе обычно{" "}
          <code>Backlog</code>: всё, что ещё не отправляли в анализ.
        </li>
        <li>
          <strong>По поиску</strong> — найти конкретный домен или
          поддомен.
        </li>
      </ul>
      <p>
        Когда отфильтровали разумный пакет (50–500 доменов), нажмите{" "}
        <strong>«Отправить все отфильтрованные на анализ»</strong> или
        выделите чекбоксами нужные строки и нажмите «Отправить на анализ».
        Drop Sherlock сразу:
      </p>
      <ul>
        <li>откроет страницу Анализа с предзаполненным списком доменов;</li>
        <li>пометит исходные строки статусом <code>In progress</code>.</li>
      </ul>
      <p>
        <strong>Удалить из Очереди.</strong> На этом же этапе можно
        чистить шум: «Удалить N выделенных» убирает отмеченные
        чекбоксами строки; <strong>«Удалить все N отфильтрованных»</strong>{" "}
        — все строки под текущим фильтром (кнопка <em>отключена</em>,
        когда ни один фильтр не активен — защита от случайного
        обнуления всей Очереди). Если домены нужно отсечь навсегда —
        bulk-action «Bulk ban» из Базы переносит их в{" "}
        <Link href="/docs/banlist">Ban List</Link> со снимком, а unban
        вернёт строку в Очередь со статусом <code>banned</code>.
      </p>

      <h2>3. Запуск анализа</h2>
      <p>
        На <Link href="/docs/analyze">странице Анализа</Link> остаётся
        выбрать:
      </p>
      <ul>
        <li>
          <strong>Критерии</strong>. По умолчанию включены четыре
          Ahrefs-критерия (B/D/A/K), Wayback и Wayback Classify
          свёрнуты — нажмите по шевр-стрелке на карточке, чтобы
          развернуть и включить. Каждый критерий — отдельный запрос
          к Ahrefs (или к Wayback) и отдельный вердикт от ИИ.
          Подробный разбор четырёх Ahrefs-критериев — в статье{" "}
          <Link href="/docs/ahrefs-criteria">«Ahrefs-критерии»</Link>.
          Если триажите домены с уже собранными Ahrefs-данными, можно
          выключить лишнее — например, оставить только Wayback
          Classify, чтобы доуточнить язык/тематику.
        </li>
        <li>
          <strong>Domain availability cascade</strong> (опт-ин).
          Первая секция на странице. Когда включено, перед Ahrefs/AI
          бэкенд проверяет доступность каскадом DNS → RDAP → Domainr →
          WHOIS. Зарегистрированные домены с истечением далеко за
          горизонт автоматически пропускаются (политика skip-registered
          в Настройках) — экономит юниты на «явно живых».
        </li>
        <li>
          <strong>ИИ-провайдер и модель</strong>. Дефолтные значения
          приходят из Настроек; на странице Анализа их можно переопределить
          поштучно. Подробнее — в статье{" "}
          <Link href="/docs/ai">«ИИ: провайдеры, модели, стоимость»</Link>.
        </li>
        <li>
          <strong>Кэш</strong>. Если вы запускаете повторный анализ тех же
          доменов в той же задаче, флажок <em>use_cache</em> переиспользует
          уже скачанные Ahrefs-данные и уже посчитанные ИИ-вердикты, если
          параметры не изменились. Если домены пришли с Базы (например,
          вы хотите перепроверить их в свежем контексте), автоматически
          ставится <em>cross_job_cache</em> — поиск ведётся по всем прошлым
          задачам. Подробности в статье <Link href="/docs/cache">«Кэш»</Link>.
        </li>
      </ul>
      <p>
        Жмёте «Запустить» — создаётся новая <strong>Задача</strong> (Job)
        с одним <strong>Запуском</strong> (Run). Дальше всё происходит на
        бэкенде.
      </p>

      <h2>4. Наблюдение за запуском</h2>
      <p>
        После сабмита вы попадаете на страницу{" "}
        <Link href="/docs/run">конкретного Запуска</Link>. Страница
        опрашивает бэкенд адаптивно: 2 с пока{" "}
        <code>running</code>, 10 с на <code>paused</code>, ноль на
        терминальных состояниях. С 2026-05-14 каждый тик идёт через
        лёгкий <code>/runs/&#123;id&#125;/progress</code> (только статус-плашки,
        ~35% от веса полного эндпоинта); полный{" "}
        <code>/runs/&#123;id&#125;</code> срабатывает сам, когда{" "}
        обнаружен переход домена в терминал или обновление{" "}
        <code>last_analyzed_at</code> — чтобы дорогие колонки
        (Verdict / Language / Theme / Category) обновились без
        задержки. Что видно в каждой строке:
      </p>
      <ul>
        <li>
          Колонка <strong>Criteria</strong> с пилюлями{" "}
          <code>B&nbsp;D&nbsp;A&nbsp;K&nbsp;W&nbsp;C</code> показывает, какие
          источники данных уже получены (зелёный = готово, синий = в
          работе, красный = ошибка). Расшифровка букв:
          <ul>
            <li>
              <code>B</code> — <strong>Backlinks</strong> (входящие ссылки)
            </li>
            <li>
              <code>D</code> — <strong>refDomains</strong> (ссылающиеся
              домены)
            </li>
            <li>
              <code>A</code> — <strong>Anchors</strong> (анкоры)
            </li>
            <li>
              <code>K</code> — <strong>Keywords</strong> (органические
              ключи)
            </li>
            <li>
              <code>W</code> — <strong>Wayback</strong> (история в архиве)
            </li>
            <li>
              <code>C</code> — wayback_<strong>C</strong>lassify (язык +
              тематика + категория)
            </li>
          </ul>
          Подробно про четыре Ahrefs-буквы — в{" "}
          <Link href="/docs/ahrefs-criteria">«Ahrefs-критерии»</Link>; про
          C — в{" "}
          <Link href="/docs/wayback-classify">«Wayback Classify»</Link>.
        </li>
        <li>
          Колонка <strong>AI Ahrefs</strong> — статус ИИ-вердиктов по тем же
          критериям B/D/A/K.
        </li>
        <li>
          В правом верхнем углу — индикатор стоимости (юниты Ahrefs и доллары
          на ИИ).
        </li>
      </ul>
      <p>
        Запуск можно <strong>поставить на паузу</strong>, <strong>отменить</strong>{" "}
        или <strong>переоценить</strong> (см. статью{" "}
        <Link href="/docs/reanalyze">«Переоценка»</Link>) — все кнопки на
        той же странице. На больших пакетах удобно открыть страницу
        Задачи параллельно: там видны несколько запусков сразу, если вы
        делали Rerun.
      </p>

      <h2>5. A/B-тестирование промптов и моделей</h2>
      <p>
        Если вы тонко настраиваете правила оценки (промпт «как считать
        ссылочный профиль низкокачественным»), хочется проверить, как новое
        правило меняет вердикты на одном и том же списке доменов. Для этого
        у задач есть Rerun:
      </p>
      <ol>
        <li>
          Откройте Задачу, нажмите <strong>«Перезапустить с новыми
          критериями»</strong>.
        </li>
        <li>
          Поменяйте промпт в{" "}
          <Link href="/docs/brain">Brain</Link> или сменив провайдера/модель.
        </li>
        <li>Запустите новый Run с включённым <em>use_cache</em>.</li>
        <li>
          На странице Задачи появится кнопка <strong>«Сравнить запуски»</strong> —
          она открывает страницу сравнения, где видно, как каждый домен
          получил вердикт в каждом запуске.
        </li>
      </ol>
      <p>
        Кэш переиспользует Ahrefs-данные, но <em>не</em> переиспользует
        ИИ-вердикт, если хеш промпта изменился — поэтому новый запуск
        обойдётся почти бесплатно по Ahrefs-юнитам, но повторно прогонит
        домены через ИИ.
      </p>

      <h2>6. Закрепление: пины по критериям + опционально Run для плашек</h2>
      <p>
        Когда у Задачи несколько запусков и вы определились, что какой
        Run даёт данные для каких критериев — открываете страницу
        нужного Run и используете панель{" "}
        <strong>«Per-criterion pins»</strong>. Каждая карточка{" "}
        (B / D / A / K / W / C) — отдельный пин. Клик закрепляет
        критерий на текущий Run в этой Задаче; повторный клик —
        снимает. Кнопка <strong>«Pin all populated»</strong> в
        той же панели закрепляет одним движением все критерии, по
        которым у этого Run есть данные.
      </p>
      <p>
        Главная сила модели — можно <em>сшивать</em> пины из разных
        Run в пределах одной Задачи. Типичный случай итеративного
        каскада: первый Run был только Wayback (дёшево, на 500 доменов),
        второй Run — Ahrefs B/D/A/K на 80 выживших. На странице
        второго Run кликаете «Pin all populated», на первом — закрепляете
        только W и C. Страница Базы по каждому домену соберёт строку
        из обоих Run.
      </p>
      <p>
        <strong>Read-only обзор пинов</strong> — на самой странице
        Задачи появился отдельный виджет «Per-criterion pins»
        (свёрнутый по умолчанию): шесть карточек, на каждой —
        ссылка на источник «B ← Run #N, W ← Run #M, …» или метка
        «not pinned». Поменять оттуда нельзя — это только обзор;
        изменения по-прежнему с самой страницы Run.
      </p>
      <p>
        Отдельный, независимый уровень — <strong>Run pin для
        плашек L/M/H</strong> в шапке Задачи. Кнопка{" "}
        <strong>«Pin»</strong> в строке Run на странице Задачи
        указывает, по какому Run считать сводку. Важно когда последний
        Run был частичным (тестировали одно изменение на 10 доменах из
        200): закрепили полный 200-доменный Run, плашки вернулись к
        полной картине. Этот пин <em>не</em> трогает данные Базы — для
        них работает только per-criterion модель выше. Подробности — в{" "}
        <Link href="/docs/pinning">«Закрепление»</Link>.
      </p>

      <h2>7. Триаж в Базе</h2>
      <p>
        Когда анализ закончен,{" "}
        <Link href="/docs/database">«База»</Link> — главный экран для
        принятия решений. В отличие от Задачи (где видно один прогон), База
        показывает все домены, которые вы когда-либо анализировали,
        перекрёстно по задачам. На каждый домен — одна строка, собранная
        из per-(Job, критерий) пинов: каждый критерий в строке может
        приходить из своего Run (см.{" "}
        <Link href="/docs/pinning">«Закрепление»</Link>). Если в Задаче
        домена для критерия пина нет, Drop Sherlock берёт самый свежий
        Run с данными по этому критерию; если домен живёт в нескольких
        Задачах с пинами — выбирается самый свежий по{" "}
        <code>updated_at</code>. Когда строка сшита из разных Run,
        страница домена показывает плашку «Stitched from Run #N (Job J)».
      </p>
      <p>
        <strong>Бейджи «Subset» и «Failed»</strong> в колонке Verdict.
        До 2026-05-14 здесь был единый «partial», теперь разделено:{" "}
        <strong>Subset</strong> (янтарный) — балл собран из подмножества
        весовых критериев, в подсказке перечислены <em>недостающие</em>{" "}
        критерии (которые надо закрепить, чтобы получить полный балл).{" "}
        <strong>Failed</strong> (красный) — у одного из критериев упал
        AI-синтез; нужен Reanalyze.
      </p>
      <p>
        Здесь фильтры играют главную роль:
      </p>
      <ul>
        <li>
          <strong>Verdict (bucket)</strong> — оставить только{" "}
          <code>good</code>, чтобы сразу видеть отобранных кандидатов.
        </li>
        <li>
          <strong>Wayback verdict</strong> — отдельный фильтр по оценке
          истории через Wayback (тематический дрифт, редиректы — частая
          причина «вроде Ahrefs хорошие, но домен умер»).
        </li>
        <li>
          <strong>Language / Theme / Category</strong> — фильтры по
          определённой языковой/тематической классификации (смотрите{" "}
          <Link href="/docs/wayback-classify">«Wayback Classify»</Link>).
          Полезно, если у вас сайт-донор на русском про недвижимость —
          вы хотите доноры в той же тематике.
        </li>
        <li>
          <strong>Provider/Model</strong> — отрезать вердикты, сделанные
          моделью, которой вы больше не доверяете.
        </li>
      </ul>
      <p>
        Колонка <strong>Criteria</strong> в Базе — это компактные зелёные
        пилюли <code>B&nbsp;D&nbsp;A&nbsp;K</code>: одна буква на каждый
        критерий, у которого собраны данные. Полный список критериев и
        количество строк видно во всплывающей подсказке.
      </p>
      <p>
        По каждому домену в строке есть две быстрые кнопки:
      </p>
      <ul>
        <li>
          <strong>Order</strong> — пометить как «нужно купить». На самом
          деле выставляет статус <code>order</code> в Очереди (даже если
          домена там ещё не было, строка создастся автоматически).
        </li>
        <li>
          <strong>Discard</strong> — пометить как «прошли мимо». Статус
          <code>discarded</code>.
        </li>
      </ul>
      <p>
        Те же кнопки есть в режиме мульти-выбора (галочки слева) — для
        пакетной обработки сразу 20–50 доменов. В bulk-плашке также
        живёт <strong>«Ban N»</strong> — переносит выделенные домены
        в <Link href="/docs/banlist">Ban List</Link> с снимком
        состояния Backlog; unban вернёт строку в Очередь со статусом{" "}
        <code>banned</code>.
      </p>

      <div className="callout callout-info docs-content">
        <p>
          <strong>Подсказка:</strong> прежде чем нажать Order, кликните на
          сам домен — откроется{" "}
          <Link href="/docs/domain">страница домена</Link> с полным
          разбором: четыре ИИ-вердикта с key_findings и red_flags, таймлайн
          Wayback-снепшотов, сырые Ahrefs-строки в табах. Тут вы видите
          обоснование оценки и можете отказаться, если ИИ переоценил
          домен.
        </p>
      </div>

      <h2>8. Возврат в Очередь: заметки, цены, заказ</h2>
      <p>
        Когда домены в Базе помечены как <code>order</code>, переходите в{" "}
        Очередь и фильтруйте по статусу <code>Order</code>. Это ваш список
        «на покупку». Здесь:
      </p>
      <ul>
        <li>
          Проставьте <strong>Желаемая&nbsp;$</strong> — ваш потолок «купить
          сразу». На бирже это будет стартовая ставка или цена в Buy Now.
        </li>
        <li>
          Проставьте <strong>Макс&nbsp;$</strong> — абсолютный максимум, выше
          которого торговаться не идёте. Помогает не подгореть на аукционе.
        </li>
        <li>
          В <strong>Комментариях</strong> — любая дополнительная информация:
          под какой проект, какой анкор планируется, источник наводки.
        </li>
      </ul>
      <p>
        Ставьте заказ на бирже или у backorder-провайдера. Когда заказ
        реально размещён (деньги/слот заняты), вернитесь в Очередь и
        переключите статус на <code>Backordered</code> («Заказан»). Если
        домен дойдёт до вас — поставьте <code>Bought</code> («Куплен»).
        Если уйдёт другому — <code>Discarded</code> или оставьте как есть
        для истории.
      </p>
      <p>
        Полный жизненный цикл статуса домена:
      </p>
      <pre><code>{`backlog → in_progress → analyzed → order → backordered → bought
                                       ↓
                                   discarded`}</code></pre>
      <p>
        Drop Sherlock не делает автоматических переходов из{" "}
        <code>order</code> в <code>backordered</code> — это всегда ручной
        флип в Очереди, потому что только вы знаете, размещён ли реально
        заказ.
      </p>

      <h2>9. Что дальше</h2>
      <p>
        Когда понадобится глубже разобраться в отдельном механизме, идите
        в соответствующую статью:
      </p>
      <ul>
        <li>
          <Link href="/docs/cache">Кэш</Link> — экономия Ahrefs-юнитов на
          повторных прогонах.
        </li>
        <li>
          <Link href="/docs/brain">Brain</Link> — как меняются веса/пороги
          итогового балла и как редактируются промпты.
        </li>
        <li>
          <Link href="/docs/pinning">Закрепление</Link> — три вида пинов и
          какой когда использовать.
        </li>
        <li>
          <Link href="/docs/reanalyze">Переоценка</Link> — когда уместно
          перезапустить один критерий, а не весь домен.
        </li>
        <li>
          <Link href="/docs/wayback-classify">Wayback Classify</Link> —
          специфика определения языка и тематики из архивных снепшотов.
        </li>
        <li>
          <Link href="/docs/banlist">Ban List</Link> — постоянный
          фильтр «не анализировать», симметричный ban/unban с
          снимком Backlog.
        </li>
        <li>
          <Link href="/docs/backups">Резервные копии</Link> — локальная
          ротация и S3-выгрузка, ежемесячный VACUUM, ретенция «недавних
          проверок» каскада.
        </li>
      </ul>
    </div>
  );
}
