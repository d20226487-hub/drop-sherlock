import Link from "next/link";

export const metadata = { title: "Очередь (Backlog) — Drop Sherlock" };

export default function BacklogDoc() {
  return (
    <div className="docs-content">
      <h1>Очередь (Backlog)</h1>
      <p>
        Очередь — это «холодный» список доменов до анализа. Сюда вы
        импортируете сырые списки с бирж и регистраторов, отсеиваете
        фильтрами и отправляете в Анализ. Сюда же возвращаетесь после
        анализа, чтобы выставить цены, проставить комментарии и отметить
        итоговый статус (заказан / куплен / отброшен).
      </p>

      <h2>Импорт</h2>
      <p>
        Кнопка <strong>«Импорт CSV»</strong> в правом верхнем углу. CSV
        должен содержать колонку <code>domain</code>; необязательные:
        <code> registrar</code>, <code>expiration_date</code>,{" "}
        <code>desired_price</code>, <code>max_price</code>,{" "}
        <code>comments</code>. Формат описан в статье{" "}
        <Link href="/docs/csv">«CSV: импорт и экспорт»</Link>.
      </p>
      <p>
        Импорт <strong>идемпотентен по домену</strong>: повторная загрузка
        тех же строк не дублирует записи. Это позволяет лить ежедневные
        выгрузки сверху, не очищая старое.
      </p>
      <p>
        Лимит на размер импорта настраивается в{" "}
        <Link href="/docs/settings">Настройках</Link> → «Other» → «Import
        limit». По умолчанию — большой запас, но имеет смысл уменьшить,
        если случайные многомегабайтные файлы не должны проходить.
      </p>

      <h2>Статусы</h2>
      <p>Каждая строка имеет один из семи статусов:</p>
      <ul>
        <li>
          <code>backlog</code> — только импортирован, не триажен.
        </li>
        <li>
          <code>in_progress</code> — отправлен в Анализ, но ещё нет
          вердикта. Drop Sherlock сам переводит сюда строки из{" "}
          <code>backlog</code> в момент отправки на Анализ.
        </li>
        <li>
          <code>analyzed</code> — анализ завершён, решение пока не принято.
          Не выставляется автоматически — вы либо отмечаете вручную, либо
          оставляете <code>in_progress</code>, а решение выносите сразу
          через Order/Discard в Базе.
        </li>
        <li>
          <code>order</code> («Order») — решили купить, заказ ещё не
          размещён. Выставляется кнопкой Order в Базе.
        </li>
        <li>
          <code>backordered</code> («Ordered») — заказ реально размещён
          (поставили в backorder, выиграли аукцион, заказ ждёт
          расчёта). Ручной флип на странице Очереди.
        </li>
        <li>
          <code>bought</code> («Bought») — домен ваш, переведён. Терминальное
          состояние.
        </li>
        <li>
          <code>discarded</code> — отказались. Терминальное.
        </li>
      </ul>
      <p>
        Drop Sherlock сознательно не делает автоматического перехода{" "}
        <code>order → backordered → bought</code>: только вы знаете, дошёл
        ли заказ. Эти флипы делаются вручную в Очереди.
      </p>

      <h2>Фильтры</h2>
      <p>Над таблицей четыре фильтра:</p>
      <ul>
        <li>
          <strong>Статус</strong> — мульти-выбор. Чтобы увидеть «всё, что
          ещё не отработано», обычно выбирают <code>backlog</code> и{" "}
          <code>in_progress</code>.
        </li>
        <li>
          <strong>Регистратор</strong> — мульти-выбор. Удобно, если работаете
          только с конкретными источниками.
        </li>
        <li>
          <strong>Дата истечения с / по</strong> — диапазон. Концентрируете
          внимание на «дропы этой недели».
        </li>
        <li>
          <strong>Поиск</strong> — по подстроке домена.
        </li>
      </ul>
      <p>
        Под фильтрами есть строка действий, относящихся{" "}
        <em>ко всему отфильтрованному набору</em>, а не только к текущей
        странице. Например, «Отправить все отфильтрованные в Анализ
        (N)» — действует на все N доменов под фильтром, даже если они на
        нескольких страницах пагинации.
      </p>

      <h2>Цены и комментарии</h2>
      <p>
        Поля <strong>Desired&nbsp;$</strong> и{" "}
        <strong>Max&nbsp;$</strong> редактируются в самой таблице — клик по
        ячейке, изменение, Enter (или клик в сторону) сохраняет. Поле{" "}
        <strong>Comments</strong> аналогично, поддерживает многострочный
        ввод.
      </p>
      <p>
        Эти поля никак не влияют на анализ — это ваши заметки, чтобы при
        выкупе не пересчитывать каждый раз стартовую и потолок.
      </p>

      <h2>Подсказка «X доменов проанализированы»</h2>
      <p>
        Иногда сверху появляется янтарная плашка типа «X доменов
        проанализированы и закреплены — отметить их статусом{" "}
        <code>analyzed</code>?». Это пассивный нудж: вы прогнали домены
        через анализ, потом закрепили их вердикт через Database (см.{" "}
        <Link href="/docs/pinning">«Закрепление»</Link>), но в Очереди их
        статус всё ещё <code>in_progress</code>. Подсказка предлагает
        одним кликом перевести их в <code>analyzed</code>. Если не хотите —
        можно скрыть на сессию.
      </p>

      <h2>Отправка в Анализ</h2>
      <p>Два пути:</p>
      <ul>
        <li>
          <strong>Через мульти-выбор</strong> — галочки слева у нужных
          строк, кнопка «Отправить N в Анализ».
        </li>
        <li>
          <strong>Через фильтры</strong> — настроили фильтры, нажали
          «Отправить все отфильтрованные в Анализ (N)». Подтверждение
          обязательно, потому что N может быть большим.
        </li>
      </ul>
      <p>
        В обоих случаях вы попадаете на{" "}
        <Link href="/docs/analyze">страницу Анализа</Link> с
        предзаполненным списком доменов. Все отправленные строки в Очереди
        получают статус <code>in_progress</code>.
      </p>

      <h2>Экспорт</h2>
      <p>
        Две кнопки экспорта в шапке:
      </p>
      <ul>
        <li>
          <strong>«Экспорт отфильтрованных (N)»</strong> — только то, что
          сейчас под фильтром.
        </li>
        <li>
          <strong>«Экспорт всех (N)»</strong> — вся Очередь, игнорируя
          фильтры.
        </li>
      </ul>
      <p>
        Файл скачивается потоково — браузер не держит весь CSV в памяти.
        Формат полей — в статье{" "}
        <Link href="/docs/csv">«CSV: импорт и экспорт»</Link>.
      </p>

      <h2>Массовые операции</h2>
      <p>В выделении нескольких строк вы можете:</p>
      <ul>
        <li>
          <strong>Сменить статус группе</strong> — например, всё выделенное
          сделать <code>discarded</code> одним нажатием.
        </li>
        <li>
          <strong>Удалить</strong> — навсегда. Подтверждение обязательно.
        </li>
        <li>
          <strong>Отправить в Анализ</strong> — то же, что в одиночной
          отправке, но пакетом.
        </li>
      </ul>
    </div>
  );
}
